import styled from 'styled-components';
export const Review = {
  Container: styled.div`
    display: flex;
    flex-direction: column;
    gap: 16px;
    align-items: flex-start;
    position: relative;
    top: -290px;
    margin-left: 150px;
  `,
};

export const Post = {};
